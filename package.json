{
  "name": "ewallet-mock-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Local mock gateway for eWallet external Bally/EDIP APIs",
  "main": "src/server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch --watch-preserve-output --watch-path=src --watch-path=.env src/server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "ldapjs": "^3.0.7"
  }
}
